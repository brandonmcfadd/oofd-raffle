<!DOCTYPE html>
<html lang="en">

<head>
  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-NLJD57NQRY"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag() { dataLayer.push(arguments); }
    gtag('js', new Date());

    gtag('config', 'G-NLJD57NQRY');
  </script>
  <link rel="apple-touch-icon" sizes="57x57" href="/apple-icon-57x57.png">
  <link rel="apple-touch-icon" sizes="60x60" href="/apple-icon-60x60.png">
  <link rel="apple-touch-icon" sizes="72x72" href="/apple-icon-72x72.png">
  <link rel="apple-touch-icon" sizes="76x76" href="/apple-icon-76x76.png">
  <link rel="apple-touch-icon" sizes="114x114" href="/apple-icon-114x114.png">
  <link rel="apple-touch-icon" sizes="120x120" href="/apple-icon-120x120.png">
  <link rel="apple-touch-icon" sizes="144x144" href="/apple-icon-144x144.png">
  <link rel="apple-touch-icon" sizes="152x152" href="/apple-icon-152x152.png">
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-icon-180x180.png">
  <link rel="icon" type="image/png" sizes="192x192" href="/android-icon-192x192.png">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="96x96" href="/favicon-96x96.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
  <link rel="manifest" href="/manifest.json">
  <meta name="msapplication-TileColor" content="#ffffff">
  <meta name="msapplication-TileImage" content="/ms-icon-144x144.png">
  <meta name="theme-color" content="#ffffff">
  <meta charset="UTF-8">
  <title>Raffle Numbers</title>
  <style>
    .logo-container {
      position: absolute;
      top: 1rem;
      right: 1rem;
    }

    .thank-you-container {
      position: fixed;
      bottom: 1rem;
      left: 0;
      width: 100%;
      text-align: center;
      color: white;
      font-size: 1rem;
    }

    a,
    a:hover,
    a:active,
    a:visited {
      color: white;
    }

    .logo {
      height: 8rem;
      padding: 0.5rem;
      width: auto;
    }

    h1 {
      color: white;
      font-size: 3rem;
      margin: 0.25rem 0;
      margin: 0.25rem 0;
      text-align: center;
    }

    h2 {
      color: white;
      font-size: 1.75rem;
      text-align: center;
      margin: 0.5rem 0;
    }

    h3 {
      color: white;
      font-size: 1.25rem;
      margin: 0.5rem 0;
      margin-bottom: 1rem;
      text-align: center;
    }

    body {
      font-family: sans-serif;
      text-align: center;
      padding: 2rem;
      padding-bottom: 4rem;
      background: black;
    }

    .main-number-container {
      background: white;
      padding: 1rem 2rem;
      border-radius: 10px;
      margin-bottom: 1rem;
      display: inline-block;
      text-align: center;
    }

    .main-number {
      font-size: 2rem;
      font-weight: bold;
      color: #d32f2f;
      margin-top: 0.5rem;
      margin-bottom: 0.5rem;
      min-width: 300px;
    }

    .main-prize {
      font-size: 1.75rem;
      color: black;
      margin-bottom: 0.5rem;
    }

    .main-prize-donor {
      font-size: 1.5rem;
      color: black;
      margin-bottom: 0.5rem;
    }

    .other-numbers {
      display: flex;
      flex-wrap: wrap;
      /* Allow items to wrap into new rows */
      justify-content: center;
      gap: 0.5rem;
      color: black;
    }

    .other-number {
      background: white;
      padding: 0.75rem;
      border-radius: 10px;
      min-width: 250px;
      max-width: 250px;
      /* Ensure some consistent size */
      text-align: center;
    }

    .other-number-number {
      font-size: 1.25rem;
      font-weight: bold;
    }

    .other-number-prize {
      font-size: 1.25rem;
      color: #444;
    }

    .other-number-donor {
      font-size: 1rem;
      color: #444;
    }

    .qr-code-container {
      display: none;
      /* hidden by default, shown only on desktop */
    }

    /* Base styles (desktop/tablet) already mostly set correctly */

    /* Responsive tweaks for mobile */
    @media (max-width: 1100px) {
      body {
        padding: 1rem;
        padding-bottom: 2rem;
      }

      h1 {
        font-size: 3rem;
      }

      h2 {
        font-size: 2.25rem;
      }

      h3 {
        font-size: 2rem;
      }

      .logo-container {
        position: relative;
        top: unset;
        right: unset;
        transform: none;
        text-align: center;
        margin-bottom: 1rem;
      }

      .logo {
        height: 10rem;
      }

      .main-number-container {
        width: 100%;
        max-width: 90%;
        margin: 1rem auto;
      }

      .other-numbers {
        flex-direction: column;
        align-items: center;
      }

      .other-number {
        width: 100%;
        max-width: 90%;
        min-width: unset;
        font-size: 1.25rem;
        margin: 0.5rem;
      }
    }

    @media (min-width: 1101px) {
      .qr-code-container {
        position: fixed;
        top: 1rem;
        left: 1rem;
        display: block;
        z-index: 1000;
        color: white;
        font-size: 1.15rem;
        /* ensure it appears above background */
      }

      .qr-code {
        width: 150px;
        height: auto;
        opacity: 0.9;
      }
    }
  </style>

</head>

<body>
  <div class="logo-container">
    <img src="/logo.png" alt="Logo" class="logo">
  </div>

  <h1>Hootenanny Raffle Numbers</h1>
  <h2>Prizes can be claimed at the prize table!</h2>
  <h3>Winners must be present to collect their prize, and prizes must be collected by 9:05pm!</h3>


  <div class="main-number-container">
    <div id="main-number" class="main-number"></div>
    <div id="main-prize" class="main-prize"></div>
    <div id="main-prize-donor" class="main-prize-donor"></div>
  </div>


  <div id="other-numbers" class="other-numbers">
    <!-- JS will populate this -->
  </div>

  <div class="qr-code-container">
    <img src="/qrcode.svg" alt="QR Code" class="qr-code">
    <p>Scan Here or visit<br>hootenanny.oofd.org<br>to check your ticket!</p>
  </div>

  <div class="thank-you-container">
    <p>Built for <a href="https://oofd.org" target="_blank">Out Our Front Door</a> with 💙 by <a href="https://github.com/brandonmcfadd/oofd-raffle" target="_blank">Brandon
        McFadden</a></p>
  </div>

  <script>
    async function fetchRaffleNumbers() {
      try {
        const response = await fetch('/api/current_numbers');
        const data = (await response.json()).slice().reverse();

        const mainNumberEl = document.getElementById('main-number');
        const mainPrizeEl = document.getElementById('main-prize');
        const mainPrizeEl2 = document.getElementById('main-prize-donor');
        const otherContainer = document.getElementById('other-numbers');

        if (Array.isArray(data) && data.length > 0) {
          // Update main number
          mainNumberEl.innerHTML = `Ticket #${data[0].number}<br>Prize #${data[0].prize_number}`;
          mainPrizeEl.innerHTML = data[0].prize;
          mainPrizeEl2.innerHTML = `<b>Donor:</b> <i>${data[0].donor}</i>`;

          // Update other numbers
          otherContainer.innerHTML = '';
          data.slice(1).forEach(entry => {
            const div = document.createElement('div');
            div.className = 'other-number';
            div.innerHTML = `
            <div class="other-number-number">Ticket #${entry.number}<br>Prize #${entry.prize_number}</div>
            <div class="other-number-prize">${entry.prize}</div>
            <div class="other-number-donor"><b>Donor:</b> <i>${entry.donor}</i></div>
          `;
            otherContainer.appendChild(div);
          });
        } else {
          // Only reset if not already in "Waiting..." state
          if (mainNumberEl.textContent !== "Waiting...") {
            mainNumberEl.textContent = "Waiting...";
            mainPrizeEl.textContent = "Drum Roll Please 🥁🙏";
            mainPrizeEl2.innerHTML = `<img src="/logo-tent.png" alt="Tent Logo" style="max-width: 250px; height: auto; margin-top: 1rem;">`;
            otherContainer.innerHTML = '';
          }
        }
      } catch (error) {
        console.error('Error fetching raffle numbers:', error);
      }
    }

    // Fetch every second
    setInterval(fetchRaffleNumbers, 1000);
    fetchRaffleNumbers();
  </script>



</body>

</html>